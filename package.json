{
    "name": "cooking-diaries-root",
    "private": true,
    "type": "module",
    "scripts": {
      "build-frontend": "cd frontend && npm install && npm run build && cd ../server && npm install",
      "start": "node server/server.js"
    }
  }
  